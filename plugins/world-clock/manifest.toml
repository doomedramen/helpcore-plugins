id = "world-clock"
name = "World Clock"
version = "0.1.1"
description = "Get the current date and time in any timezone, or convert a date/time between timezones — no API key required"
tier = "wasm"
permissions = ["outbound_http"]
allowed_hosts = [
    "timeapi.io",
]

brief = "Use when the user asks what time it is in a specific city or country, to convert a time between timezones, or about timezone differences."
