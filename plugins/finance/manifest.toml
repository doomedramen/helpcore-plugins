id = "finance"
name = "Finance"
version = "0.1.1"
description = "Stock and cryptocurrency price quotes — no API key required"
tier = "wasm"
permissions = ["outbound_http"]
allowed_hosts = [
    "query1.finance.yahoo.com",
    "api.coingecko.com",
]

brief = "Use when the user asks for a stock price by ticker symbol or a cryptocurrency price by name or symbol."
