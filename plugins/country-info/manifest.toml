id = "country-info"
name = "Country Info"
version = "0.1.1"
description = "Look up information about countries — population, capital, currency, flag, languages, and more"
tier = "wasm"
permissions = ["outbound_http"]
allowed_hosts = [
    "restcountries.com",
]

brief = "Use when the user asks for facts about a specific country — population, capital, currency, language, timezone, or border information."
