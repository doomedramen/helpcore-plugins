id = "holiday-check"
name = "Holiday Check"
version = "0.1.2"
description = "Look up public holidays for any country — full-year listings or what's coming up next — no API key required"
tier = "wasm"
permissions = ["outbound_http"]
allowed_hosts = [
    "date.nager.at",
]

brief = "Use when the user asks about public holidays — what holidays a country has in a given year or what holidays are coming up."
