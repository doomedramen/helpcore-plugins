id = "meal-finder"
name = "Meal Finder"
version = "0.1.1"
description = "Search for recipes by name, ingredient, or category — get full instructions and ingredient lists — no API key required"
tier = "wasm"
permissions = ["outbound_http"]
allowed_hosts = [
    "www.themealdb.com",
]
