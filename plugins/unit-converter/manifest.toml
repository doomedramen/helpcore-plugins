id = "unit-converter"
name = "Unit Converter"
version = "0.1.1"
description = "Convert between common units — length, weight, temperature, volume, speed, data size, and more"
tier = "wasm"
permissions = []

brief = "Use when the user asks to convert between units — length, weight, temperature, volume, speed, data storage, area, time, pressure, energy, or angle."
