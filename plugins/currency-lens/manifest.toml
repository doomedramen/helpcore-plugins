id = "currency-lens"
name = "Currency Lens"
version = "0.1.1"
description = "Convert between currencies and check live exchange rates, backed by European Central Bank reference data — no API key required"
tier = "wasm"
permissions = ["outbound_http"]
allowed_hosts = [
    "api.frankfurter.dev",
]

brief = "Use when the user asks to convert an amount between currencies or check the current exchange rate between two currencies."
