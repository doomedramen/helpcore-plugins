id = "translation"
name = "Translation"
version = "0.1.1"
description = "Translate text between languages and detect languages — powered by LibreTranslate"
tier = "wasm"
permissions = ["outbound_http"]
allowed_hosts = [
    "translate.argosopentech.com",
    "libretranslate.com",
]

brief = "Use when the user asks to translate text between languages or detect what language a text is written in."
