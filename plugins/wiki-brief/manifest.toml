id = "wiki-brief"
name = "Wiki Brief"
version = "0.1.1"
description = "Get concise, plain-text Wikipedia summaries for any topic, person, or place — no API key required"
tier = "wasm"
permissions = ["outbound_http"]
allowed_hosts = [
    "en.wikipedia.org",
]

brief = "Use when the user asks for a quick Wikipedia summary — who someone is, what something is, or a brief explanation of a topic."
