id = "dns-peek"
name = "DNS Peek"
version = "0.1.2"
description = "Look up DNS records for any domain — A, AAAA, MX, TXT, NS, CNAME and more — via Cloudflare's DNS-over-HTTPS resolver, no API key required"
tier = "wasm"
permissions = ["outbound_http"]
allowed_hosts = [
    "cloudflare-dns.com",
]

brief = "Use when the user asks about DNS records for a domain — IP address, mail server, name server, or TXT records."
