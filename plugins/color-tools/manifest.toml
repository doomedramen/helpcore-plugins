id = "color-tools"
name = "Color Tools"
version = "0.1.1"
description = "Convert between color formats (hex, RGB, HSL, CMYK) and generate color harmonies"
tier = "wasm"
permissions = ["outbound_http"]
allowed_hosts = [
    "www.thecolorapi.com",
]

brief = "Use when the user asks to convert between color formats, generate color palettes or harmonies, or identify a color name from a hex value."
