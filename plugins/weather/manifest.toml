id = "weather"
name = "Weather"
version = "0.2.3"
description = "Weather forecasts and current conditions for any location worldwide — no API key required"
tier = "wasm"
permissions = ["outbound_http"]
allowed_hosts = [
    "api.open-meteo.com",
    "geocoding-api.open-meteo.com",
]

brief = "Use when the user asks about current conditions, temperature, or weather forecast for any location."
