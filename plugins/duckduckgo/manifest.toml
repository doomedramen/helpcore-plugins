id = "duckduckgo"
name = "DuckDuckGo Search"
version = "0.1.2"
description = "Search the web using DuckDuckGo — returns ranked results with titles, URLs, and snippets. No API key needed."
tier = "wasm"
permissions = ["outbound_http"]
allowed_hosts = ["html.duckduckgo.com"]

brief = "Use when the user asks for current information, recent events, web search results, or anything requiring live internet knowledge beyond your training data."
